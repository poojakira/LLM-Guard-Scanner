# Runtime requirements for llm-guard-scanner core (no heavy deps).
# For API serving: pip install -e ".[api]"
# For ML detectors: pip install -e ".[ml]"
# For development: pip install -e ".[dev]"
# See pyproject.toml for the full dependency graph.
